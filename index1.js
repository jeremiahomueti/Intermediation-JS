//Difference Number 2: Whenever the function only has one argument, the round bracket becomes optional.

//So a function to add 2 as seen below:

function add2(num)
{
   return num += 2;
}

console.log(add2(5));